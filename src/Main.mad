import IO from "IO"

import {
  Bronze,
  FAILURE_CASE,
  Gold,
  NUMBERS_AND_WORDS,
  Numeric,
  Silver,
  Tin,
  Word,
} from "./ButterLists"



main = () => {
  IO.pTrace("NUMBS Should have no Tin", NUMBERS_AND_WORDS)
  IO.pTrace("When FAILURE_CASE is printed though", FAILURE_CASE)
}
